# TripMate app smoke compose — API + Web + Postgres + RustFS.
# `docs/runbooks/docker-app.md`.

services:
  app-postgres:
    image: postgis/postgis:16-3.5-alpine
    environment:
      POSTGRES_USER: tripmate
      POSTGRES_PASSWORD: tripmate_app_smoke
      POSTGRES_DB: tripmate
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U tripmate -d tripmate"]
      interval: 5s
      timeout: 5s
      retries: 10

  app-rustfs:
    image: rustfs/rustfs:latest
    environment:
      RUSTFS_ACCESS_KEY: rustfsadmin
      RUSTFS_SECRET_KEY: rustfsadmin
      RUSTFS_VOLUMES: /data
      RUSTFS_ADDRESS: ":12101"
      RUSTFS_CONSOLE_ENABLE: "true"
      RUSTFS_CONSOLE_ADDRESS: ":12105"
    ports:
      - "${TRIPMATE_RUSTFS_PORT:-12101}:12101"
      - "${TRIPMATE_RUSTFS_CONSOLE_PORT:-12105}:12105"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:12101/health/live || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 10

  app-rustfs-init:
    image: minio/mc:latest
    depends_on:
      app-rustfs:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://app-rustfs:12101 rustfsadmin rustfsadmin;
      mc mb -p local/tripmate-media || true;
      "

  app-api:
    build:
      context: ..
      dockerfile: apps/api/Dockerfile
    image: tripmate-api:local
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      app-postgres:
        condition: service_healthy
      app-rustfs:
        condition: service_healthy
    environment:
      TRIPMATE_ENVIRONMENT: smoke
      TRIPMATE_DATABASE_URL: postgresql+asyncpg://tripmate:tripmate_app_smoke@app-postgres:5432/tripmate
      TRIPMATE_JWT_SECRET_KEY: tripmate-smoke-jwt-secret-32-bytes-minimum
      TRIPMATE_RESEND_API_KEY: ""
      TRIPMATE_RUSTFS_ENDPOINT_URL: http://app-rustfs:12101
      TRIPMATE_RUSTFS_PUBLIC_ENDPOINT_URL: http://127.0.0.1:12101
      TRIPMATE_RUSTFS_BUCKET: tripmate-media
      TRIPMATE_RUSTFS_ACCESS_KEY_ID: rustfsadmin
      TRIPMATE_RUSTFS_SECRET_ACCESS_KEY: rustfsadmin
      TRIPMATE_AGENT_API_BASE_URL: http://host.docker.internal:12401
      TRIPMATE_CORS_ALLOWED_ORIGINS: '["http://127.0.0.1:12505"]'
    ports:
      - "${TRIPMATE_API_PORT:-12501}:8000"

  app-web:
    build:
      context: ..
      dockerfile: apps/web/Dockerfile
      args:
        NEXT_PUBLIC_TRIPMATE_API_URL: http://127.0.0.1:${TRIPMATE_API_PORT:-12501}
    image: tripmate-web:local
    depends_on:
      - app-api
    ports:
      - "${TRIPMATE_WEB_PORT:-12505}:3000"
