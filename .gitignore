# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
build/
dist/
.eggs/
.installed.cfg

# venv
.venv/
.venv-wsl/
venv/
env/
apps/api/.venv/
apps/api/.venv-wsl/

# Node
node_modules/
.next/
out/
tsconfig.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# caches
.mypy_cache/
.pytest_cache/
.ruff_cache/
.import_linter_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Office / Word lock files
~$*
*.tmp

# env / secrets
.env
.env*.local
*.local.md
# 운영(prod) env — 실제 도메인/시크릿. 공개 repo에 커밋 금지. 템플릿은 *.example만 추적.
.env.prod
.env.production
# mcp-telegram local credential (per-worktree, never committed) — ADR-016/PR mcp-telegram
.env.mcp-telegram
# Local-only 민감 노트(prod 접속·배포 런북·반복 실수 로그 등) — 커밋 금지. per-worktree 수동 복사.
# AGENTS.md "prod 배포 & 보안 감사" 참조. kor-travel-concierge 동일 패턴.
*.local.md
docs/deploy-runbook.local.md
docs/prod-access.local.md
.local/

# data / artifacts (kept on NTFS, not in git per dev-environment 정책)
dataset/
testset/
refdocs/
test-results/
data/
artifacts/
*.log

# Local tool state
.dagster/
.tmp/

# Agent local state
.codex_tmp/

# CodeGraph 인덱스 (ADR-017) — SQLite + FTS5, worktree마다 별개
.codegraph/

# Build outputs
build/
dist/

# Claude Code 로컬 전용(공유 X) + 런타임 산출물
.claude/settings.local.json
.claude/scheduled_tasks.lock
